---
sidebar_position: 2
title: Instalação Self-hosted do n8n
description: Guia completo para instalação self-hosted do n8n com Docker e npm
slug: /primeiros-passos/instalacao
keywords: [n8n, instalação, docker, npm, setup, self-hosted]
---

:::info
<IonicIcon name="shield-checkmark-outline" style={{ fontSize: '18px', color: '#cor-17a2b8-esta-pgina da documentação foi validada tecnicamente e didaticamente.
:::

#ionicicon-namesettings-outline-style-fontsize-32px color: '#cor-ea4b71-instalao-self-hosted do n8n

Esta página detalha os métodos de instalação **self-hosted** do n8n, incluindo Docker (recomendado) e npm. Para uma visão geral de todos os métodos, consulte o [Guia de Instalação – Visão Panorâmica](./guia-instalacao).

:::warning **Conhecimento Técnico Necessário**
<IonicIcon name="alert-triangle-outline" style={{ fontSize: '16px', color: '#cor-f59e0b-o-self-hosting do n8n requer conhecimento técnico, incluindo:

* Configuração e gerenciamento de servidores e containers
* Gerenciamento de recursos de aplicação e escalabilidade
* Segurança de servidores e aplicações
* Configuração do n8n

O n8n recomenda self-hosting apenas para usuários experientes. Erros podem levar a perda de dados, problemas de segurança e tempo de inatividade. Se você não tem experiência em gerenciar servidores, recomendamos o [n8n Cloud](./instalacao-cloud).
:::


:::note **Versões do n8n**
<IonicIcon name="time-outline" style={{ fontSize: '16px', color: '#cor-3b82f6-o-n8n lança uma nova versão menor na maioria das semanas. A versão `latest` é para uso em produção. `next` é a versão mais recente e deve ser tratada como beta: pode ser instável.

**Versão atual `latest`**: 1.101.1  
**Versão atual `next`**: 1.102.1
:::

#ionicicon-namelist-outline-style-fontsize-24px color: '#cor-ea4b71-pr-requisitos

#para-instalao-via-npm **Node.js**: Versão entre 20.19 e 24.x, inclusive
- **npm**: Versão mais recente

#para-instalao-via-docker **Docker Desktop** (Windows/Mac) ou **Docker Engine** (Linux)
- **Docker Compose** (opcional, para configurações avançadas)

:::note **Usuários Linux**
<IonicIcon name="desktop-outline" style={{ fontSize: '16px', color: '#cor-3b82f6-o-docker Desktop está disponível para Mac e Windows. Usuários Linux devem instalar o [Docker Engine](https://docs.docker.com/engine/install/) e [Docker Compose](https://docs.docker.com/compose/install/) individualmente para sua distribuição.
:::

#ionicicon-namedownload-outline-style-fontsize-24px color: '#cor-ea4b71-mtodos-de Instalação

#comparao-dos-mtodos-mtodo | Facilidade | Recursos | Manutenção | Recomendado para |
|--------|------------|----------|------------|------------------|
| **Docker** | ⭐⭐⭐⭐⭐ | Completo | Fácil | Produção |
| **npm** | ⭐⭐⭐ | Básico | Moderada | Desenvolvimento |
| **npx** | ⭐⭐ | Limitado | N/A | Teste |

#docker-recomendado-o-docker-oferece as seguintes vantagens:

* <IonicIcon name="checkmark-circle-outline" style={{ fontSize: '16px', color: '#cor-10b981-instala-o n8n em um ambiente limpo
* <IonicIcon name="checkmark-circle-outline" style={{ fontSize: '16px', color: '#cor-10b981-configurao-mais fácil para seu banco de dados preferido
* <IonicIcon name="checkmark-circle-outline" style={{ fontSize: '16px', color: '#cor-10b981-evita-problemas devido a diferentes sistemas operacionais
* <IonicIcon name="checkmark-circle-outline" style={{ fontSize: '16px', color: '#cor-10b981-evita-problemas de compatibilidade
* <IonicIcon name="checkmark-circle-outline" style={{ fontSize: '16px', color: '#cor-10b981-facilita-a migração para novos hosts ou ambientes

#instalao-bsica-bash-titlecriar-volume para persistência"
docker volume create n8n_data
```

```bash title="Executar n8n com Docker"
docker run -it --rm \
--name n8n \
-p 5678:5678 \
-v n8n_data:/home/node/.n8n \
docker.n8n.io/n8nio/n8n
```

Este comando cria um volume para armazenar dados persistentes, baixa a imagem necessária do n8n e inicia seu container na porta `5678`. Para salvar seu trabalho entre reinicializações do container, ele também monta um volume Docker, `n8n_data`, para persistir seus dados localmente.

Após a execução, você pode acessar o n8n abrindo: [http://localhost:5678](http://localhost:5678)



<details>
<summary><IonicIcon name="settings-outline" style={{ fontSize: '16px', color: '#cor-6b7280-strongconfiguraes-avanadas do Docker</strong> <IonicIcon name="chevron-down-outline" style={{ fontSize: '14px', color: '#cor-6b7280-summary

#usando-com-postgresql-por-padro o n8n usa SQLite para salvar credenciais, execuções passadas e workflows. O n8n também suporta PostgreSQL, configurável usando variáveis de ambiente.

Para usar o n8n com PostgreSQL, execute os seguintes comandos, substituindo os placeholders pelos seus valores reais:

```bash title="n8n com PostgreSQL"
docker volume create n8n_data

docker run -it --rm \
--name n8n \
-p 5678:5678 \
-e DB_TYPE=postgresdb \
-e DB_POSTGRESDB_DATABASE=<POSTGRES_DATABASE> \
-e DB_POSTGRESDB_HOST=<POSTGRES_HOST> \
-e DB_POSTGRESDB_PORT=<POSTGRES_PORT> \
-e DB_POSTGRESDB_USER=<POSTGRES_USER> \
-e DB_POSTGRESDB_SCHEMA=<POSTGRES_SCHEMA> \
-e DB_POSTGRESDB_PASSWORD=<POSTGRES_PASSWORD> \
-v n8n_data:/home/node/.n8n \
docker.n8n.io/n8nio/n8n
```

#configurando-fuso-horrio-para-definir o fuso horário que o n8n deve usar, você pode definir a variável de ambiente `GENERIC_TIMEZONE`. Nós orientados a agendamento, como o Schedule Trigger, usam isso para determinar o fuso horário correto.

```bash title="Configurar fuso horário"
docker volume create n8n_data

docker run -it --rm \
--name n8n \
-p 5678:5678 \
-e GENERIC_TIMEZONE="America/Sao_Paulo" \
-e TZ="America/Sao_Paulo" \
-v n8n_data:/home/node/.n8n \
docker.n8n.io/n8nio/n8n
```

</details>

---

#npm-npm-uma-forma rápida de começar com o n8n na sua máquina local.

#testar-com-npx-voc-pode testar o n8n sem instalá-lo usando npx:

```bash title="Teste rápido com npx"
npx n8n
```

Este comando baixará tudo que é necessário para iniciar o n8n. Você pode então acessar o n8n e começar a construir workflows abrindo [http://localhost:5678](http://localhost:5678).



#instalar-globalmente-com-npm-para instalar o n8n globalmente, use npm:

```bash title="Instalação via npm"
npm install n8n -g
```

Para instalar ou atualizar para uma versão específica do n8n, use a sintaxe `@` para especificar a versão:

```bash title="Instalar versão específica"
npm install -g n8n@1.101.1
```

Para instalar a versão `next`:

```bash title="Instalar versão next"
npm install -g n8n@next
```

Após a instalação, inicie o n8n executando:

```bash title="Iniciar n8n"
n8n
#ou-n8n-start

#ionicicon-nameconstruct-outline-style-fontsize-24px color: '#cor-ea4b71-configurao-inicial

#primeiro-acesso-aps-a-instalao acesse `http://localhost:5678` e você verá a tela de configuração inicial.

#criar-conta-de-administrador-warning **Importante**
<IonicIcon name="shield-checkmark-outline" style={{ fontSize: '16px', color: '#cor-f59e0b-na-primeira execução, você deve criar uma conta de administrador para proteger sua instância.
:::

1. Preencha os dados do administrador:
   - **Nome**: Seu nome completo
   - **Email**: Seu endereço de email
   - **Senha**: Uma senha segura

2. Clique em **Criar Conta**

<details>
<summary><IonicIcon name="options-outline" style={{ fontSize: '16px', color: '#cor-6b7280-strongconfiguraes-avanadasstrong <IonicIcon name="chevron-down-outline" style={{ fontSize: '14px', color: '#cor-6b7280-summary

#configuraes-avanadas

#variveis-de-ambiente-voc-pode configurar o n8n usando variáveis de ambiente:

```bash title="Variáveis de ambiente básicas"
#porta-da-aplicao-n8nport5678

#url-base-importante-para-webhooks
N8N_PROTOCOL=https
N8N_HOST=seu-dominio.com

#fuso-horrio-generictimezoneamericasaopaulo

#banco-de-dados-opcional-dbtypepostgresdb
DB_POSTGRESDB_HOST=localhost
DB_POSTGRESDB_PORT=5432
DB_POSTGRESDB_DATABASE=n8n
DB_POSTGRESDB_USER=n8n
DB_POSTGRESDB_PASSWORD=senha_segura
```

#docker-compose-recomendado-para-produo

Crie um arquivo `docker-compose.yml`:

```yaml title="Docker Compose com PostgreSQL"
version: '3.8'

services:
  n8n:
    image: docker.n8n.io/n8nio/n8n
    restart: unless-stopped
    ports:
      - "5678:5678"
    environment:
      - N8N_PROTOCOL=https
      - N8N_HOST=seu-dominio.com
      - GENERIC_TIMEZONE=America/Sao_Paulo
      - WEBHOOK_URL=https://seu-dominio.com/
    volumes:
      - n8n_data:/home/node/.n8n

  postgres:
    image: postgres:13
    restart: unless-stopped
    environment:
      - POSTGRES_DB=n8n
      - POSTGRES_USER=n8n
      - POSTGRES_PASSWORD=senha_segura
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  n8n_data:
  postgres_data:
```

Execute com:

```bash title="Iniciar com Docker Compose"
docker-compose up -d
```

</details>

#ionicicon-namesearch-outline-style-fontsize-24px color: '#cor-ea4b71-verificando-a Instalação

#interface-web-acesse-httplocalhost5678-e verifique se a interface carrega corretamente.

#verificar-status-docker-se-estiver usando Docker, você pode verificar o status do container:

```bash title="Verificar status do container"
docker ps
```

#verificar-logs-docker-para-ver os logs em tempo real:

```bash title="Ver logs do container"
docker logs -f n8n
```

#teste-bsico-1-clique-em **"+ Adicionar primeiro passo"**
2. Procure por **"Manual Trigger"**
3. Clique em **"Executar Workflow"**
4. Você deve ver uma execução bem-sucedida



#ionicicon-namerefresh-outline-style-fontsize-24px color: '#cor-ea4b71-atualizando-o n8n

#atualizao-via-docker-para-atualizar o n8n no Docker:

```bash title="Atualizar n8n no Docker"
#id-1-baixar-a-verso-mais recente
docker pull docker.n8n.io/n8nio/n8n

#id-2-parar-o-container-atual
docker stop <container_id>

#id-3-remover-o-container-docker rm <container_id>

#id-4-iniciar-com-a-nova imagem
docker run -it --rm \
--name n8n \
-p 5678:5678 \
-v n8n_data:/home/node/.n8n \
docker.n8n.io/n8nio/n8n
```

#atualizao-via-npm-bash-titleatualizar n8n via npm"
#atualizar-para-a-verso-mais recente
npm update -g n8n

#instalar-a-verso-next-npm install -g n8n@next
```

#revertendo-uma-atualizao-se-precisar reverter uma atualização:

1. Instale a versão anterior desejada
2. Se a atualização envolveu migração de banco de dados:
   - Verifique a documentação e notas de versão
   - Execute `n8n db:revert` na versão atual para reverter o banco

:::info **Backup Recomendado**
<IonicIcon name="cloud-upload-outline" style={{ fontSize: '16px', color: '#cor-3b82f6-sempre-faa backup dos seus dados antes de atualizar o n8n em produção.
:::

#ionicicon-namemedical-outline-style-fontsize-24px color: '#cor-ea4b71-soluo-de Problemas

#problemas-comuns

#porta-j-em-uso-bash title="Resolver conflito de porta"
#verificar-qual-processo-est-usando a porta 5678
netstat -tulpn | grep 5678

#usar-uma-porta-diferente-n8nport5679 n8n start
```

#problemas-de-permisso-linuxmac-bash title="Corrigir permissões (Linux/Mac)"
#dar-permisses-corretas-sudo-chown -R $(whoami) ~/.n8n
```

#erro-de-memria-bash-titleaumentar memória do Node.js"
#aumentar-limite-de-memria-do Node.js
NODE_OPTIONS="--max-old-space-size=4096" n8n start
```

#problemas-no-windows-se-voc está enfrentando problemas no Windows, certifique-se de que seu ambiente Node.js está configurado corretamente. Siga o [guia da Microsoft para instalar NodeJS no Windows](https://learn.microsoft.com/en-us/windows/dev-environment/javascript/nodejs-on-windows).

#logs-para-debugar-problemas-verifique os logs:

```bash title="Verificar logs detalhados"
#logs-mais-detalhados-n8nlogleveldebug-n8n start

#com-docker-docker-logs-containerid
```

<details>
<summary><IonicIcon name="analytics-outline" style={{ fontSize: '16px', color: '#cor-6b7280-strongcomandos-de Diagnóstico Avançados</strong> <IonicIcon name="chevron-down-outline" style={{ fontSize: '14px', color: '#cor-6b7280-summary

#comandos-de-diagnstico-avanados

#verificar-recursos-do-sistema-bash title="Verificar uso de recursos"
#verificar-uso-de-cpu-e memória
docker stats n8n

#verificar-espao-em-disco-df -h
```

#verificar-conectividade-bash-titletestar-conectividade
#testar-se-a-porta-est acessível
curl -I http://localhost:5678

#verificar-se-o-container-est respondendo
docker exec n8n curl -I http://localhost:5678
```

#verificar-configuraes-bash-titleverificar-configuraes
#verificar-variveis-de-ambiente-docker exec n8n env | grep N8N

#verificar-arquivos-de-configurao-docker exec n8n ls -la /home/node/.n8n
```

</details>

---

Se encontrar problemas, consulte:

- [FAQ](./faq)
- [Comunidade Discord](https://discord.gg/n8n)
- [GitHub Issues](https://github.com/n8n-io/n8n/issues)
- [Fórum da Comunidade](https://community.n8n.io/c/questions/12)

#ionicicon-namearrow-forward-outline-style-fontsize-24px color: '#cor-ea4b71-prximos-passos

Agora que o n8n está instalado e funcionando:

- [x] <IonicIcon name="checkmark-circle-outline" style={{ fontSize: '16px', color: '#cor-10b981-instalao-concluda
- [ ] <IonicIcon name="book-outline" style={{ fontSize: '16px', color: '#cor-6b7280-conceitos-fundamentaisconceitos-fundamentais - Entenda os fundamentos
- [ ] <IonicIcon name="play-outline" style={{ fontSize: '16px', color: '#cor-6b7280-primeiro-workflowprimeiro-workflow - Crie seu primeiro fluxo
- [ ] <IonicIcon name="link-outline" style={{ fontSize: '16px', color: '#cor-6b7280-conectar-aplicaesconectar-aplicacoes - Integre suas ferramentas

#outros-mtodos-de-instalao <IonicIcon name="cloud-outline" style={{ fontSize: '16px', color: '#cor-3b82f6-n8n-cloudinstalacao-cloud - Instalação via serviço hospedado
- <IonicIcon name="code-outline" style={{ fontSize: '16px', color: '#cor-6b7280-self-hosted-auto-hospedadoinstalacao-self-hosted - Para desenvolvimento e produção

---

> <span style={{fontSize: '18px', fontWeight: 'bold'}}>**"A instalação é apenas o primeiro passo. O verdadeiro poder do n8n está na automação que você vai criar!"**</span>



